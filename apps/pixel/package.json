{
  "name": "@sparkle/pixel-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "pnpm --filter \"@sparkle/pixel-service^...\" build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@sparkle/http": "workspace:*",
    "@sparkle/kernel": "workspace:*",
    "@sparkle/pixel-api": "workspace:*",
    "fastify": "^5.6.2",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/pngjs": "^6.0.5",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
