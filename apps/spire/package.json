{
  "name": "@sparkle/spire-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "pnpm --filter \"@sparkle/spire-service^...\" build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@sparkle/http": "workspace:*",
    "@sparkle/kernel": "workspace:*",
    "@sparkle/spire-api": "workspace:*",
    "@kisinwen/sts-engine": "0.5.0",
    "fastify": "^5.6.2",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
