# 设计系统 — Sparkle 镜

> 方向代号：**鲜艳的蒙德里安 / The Painted Ledger**（饱和原色 + 二维色块构图）
> 任何视觉 / UI 决策前先读本文件。未经明确批准不要偏离。
> 本设计系统由 `/design-consultation` 产出（2026-06-30），并刻意**不再使用 shadcn 的有样式组件与默认 slate 主题**——保留 Radix 无样式行为基元（管焦点 / 键盘可访问性），其余 class 体系与组件外观全部自定义。

## 产品语境

- **这是什么：** Sparkle 的 Web 管理台，是一扇观察「一个同事式 Agent」工作状态的窗。不是聊天机器人 UI。
- **谁在用：** 仅创建者一人（私人观测台，不对外）。
- **品类 / 邻居：** 开发者观测台 / Agent 运行时仪表盘（精神近邻：Linear、Grafana 的克制，但刻意不抄它们的视觉语言）。
- **项目类型：** 数据密集的内部 Web 仪表盘。承载中文（CJK），到处是 ID、时间戳、token 计数、JSON。

## 美术方向

- **方向：** 新造型主义（蒙德里安）——饱和原色大色块 + `2px` 黑硬线，二维横竖切分整张画布。参照 Mondrian《Composition with Red, Blue and Yellow》：饱和红蓝黄、黑格、中性留白。
- **统一法则（不可动摇）：**
  - **几何永远利落**——网格 / 分隔线 / 面板边界一律 `2px` 暖近黑硬线，永不模糊、永不圆角堆叠。
  - **颜色饱和上墙**——蒙德里安原色（饱和红蓝黄绿玫红），以**填实色块**承载；**色块内永不做渐变**，不靠阴影 / 纹理 / blob 装饰。
  - **二维构图**：页面是一块画布，可横向、纵向分块；大小不一的矩形咬合切分（gap-0 共享黑线），不是从上往下的等宽卡片流。
- **装饰层级：** intentional——靠饱和色块 + 硬线构图本身，不堆装饰。
- **心情：** 第一眼「这像一幅蒙德里安，不像一个后台」——鲜艳大胆，然后发现它是活的密集仪表，「能读数据的画」。
- **配色铁律：** 颜色是**结构，不是配给**。语义色以**填实色块 / 状态格 / 大数字**上墙，铺到 chrome（侧栏选中、统计块、状态徽章）；中性只留给底色与长正文。数据概览 / 统计性质的面板优先做成**填实大色块**。

## 字体

- **Display / 大数字（拉丁）：** Fraunces — 变量光学字号，文艺复兴刻字般的笔锋，承载「绘画」气质；用于页面标题、关键状态名、大统计数字，不大面积使用。
- **CJK 标题：** 思源宋体 Noto Serif SC — 宋体的庄重与老大师同源；**宋体做中文标题是本系统的标志性冒险**，让数 token 的工具读起来像 Agent 在写日记。
- **正文（拉丁）：** Literata — 纸本文字感，适合观测日志与长说明。
- **正文 / UI 标签（CJK 密集）：** 思源黑体 Noto Sans SC。
- **数据 / 表格 / 代码（必须 tabular-nums）：** JetBrains Mono — 承载所有 ID / token / 时间戳 / JSON / provider payload。数据是 Sparkle 的「笔触」，不藏，用漂亮等宽呈现，让技术密度本身成为审美。
  - 备选升级（付费）：Commit Mono；拉丁正文亦可换 Söhne / 标题换 Cormorant Garamond / GT Sectra（同档审美）。
- **加载：** Google Fonts（或 Bunny Fonts）`<link>`；CJK 子集化按需自托管以控体积。
- **字号刻度（rem，base 16px）：** 12 / 13 / 15（正文）/ 17 / 20 / 24（区块标题）/ 34（中文 display）/ 52（拉丁 display）。

## 色彩

- **取向：** saturated-semantic（饱和原色填实上墙，一块色 = 一种含义；用法以填实色块为主，避免把饱和色当浅底上的小字）。
- **语义映射（浅色）：**
  - 正红 `#C92414` — 错误 · 主动事件 · Agent 发言（晒深至白字 AA）
  - 正蓝 `#143CB0` — LLM · 推理 · context · provider
  - 正黄 `#F7C400` — scheduler · 等待 · pending（**黄底必配黑字**，绝不作浅底文字）
  - 正绿 `#22773D` — Story · 记忆 · recall（晒深至白字 AA）
  - 玫红 `#B61E3C` — 高 token 成本 · 风险信号
- **图表扩展序列色（仅折线图多系列回落，无语义、不上墙）：** 图表前 4 个系列用语义原色 token（蓝/红/绿/玫红，`hsl(var(--llm/--signal/--story/--cost))`）；第 5 个系列起按序回落到去饱和扩展色：深赭黄 `#C9892E` · 暗绿松石 `#3F6B68` · 灰紫 `#6B5D82` · 赭石棕 `#8A5A38`。这 4 个扩展色只允许出现在图表 series 里，不承载语义、不得用于徽章/色块/文字。
- **填实块文字（WCAG AA 铁律）：** **浅色**填实块配近白字（`#F1EBDF`，红/绿已晒深至白字达 4.5:1）；**深色**填实块原色提亮、一律**配骨黑字**（亮色上黑字对比更高、更跳）；黄底永远黑字。色块大字（≥18.66px 粗或 ≥24px）按大文本 3:1，小徽章按 4.5:1。
- **中性（浅色 / 读档案）：** 底 `#F1EBDF`（石膏暖白）· 面 `#FAF6EE`（亚麻）· 凸面 `#EFE3D0` · 细线 `#D9CBB6` · 弱字 `#6E6456`（生赭，AA）· 墨 `#1C1814`（骨黑，非纯黑）。色块边框用墨色 `2px`。
- **深色 / 守夜（黑底点亮原色，非反相）：** 底 `#15110C` · 面 `#201B14` · 凸面 `#2A2218` · 细线 `#382E22` · 弱字 `#8E8170` · 墨 `#ECE4D2`（烛光铅白）。原色提亮（配黑字）：红 `#E8412E` · 蓝 `#5B82E6` · 黄 `#FBC81E` · 绿 `#3FA862` · 玫红 `#E26A88`。
- **主用模式：** 浅色（`:root` 默认，对应当前前端）；深色作为 `.dark` 完整重设（非简单取反），随时可切。

## 间距

- **基础单位：** 4px。
- **密度：** comfortable（观测台偏紧，但留出画布般的外边距）。
- **刻度：** 2 / 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64。

## 布局

- **取向：** 二维蒙德里安构图——一块画布**横向 + 纵向**分块，大小不一的矩形咬合切分（`gap-0`、相邻面板共享 `2px` 黑线），**不是从上往下的等宽卡片流**。失衡是刻意的：大块统计 / live 面占视觉重心，次要面板缩放环绕。
- **大色块上墙：** 数据概览 / 统计 / 额度这类面板做成**填实饱和色块**（彩底 + 白/黑字 + 大号 mono 数字），作为构图的色彩主体；读取流 / 表格 / 长正文留在中性面板里。
- **网格线：** `2px` 暖近黑硬线作为结构本身（蒙德里安线），满铺咬合。面板边界即构图。
- **侧边导航：** 一条暖近黑「画布边」竖栏，选中项是钉在黑栏上的**正黄色块**（黑字）。
- **最大内容宽：** ~1180px。
- **圆角：** 结构层 **0**（硬朗）；重复列表行最多 `4px`；不做统一气泡圆角。

## 动效

- **取向：** minimal-functional——只保留帮助理解的过渡。颜料色块的出现 / 消失要果断，不做花哨编排。
- **缓动：** 入 ease-out / 出 ease-in / 位移 ease-in-out。
- **时长：** micro 50–100ms · short 150–250ms · medium 250–400ms。

## 实现备注（去 shadcn）

- 保留 `@radix-ui/*` 无样式基元（dialog / select 的焦点陷阱与键盘可访问性），**移除 shadcn 的有样式组件层与默认 slate 主题**。`class-variance-authority` 作为中性的 class 组合工具保留（button / badge 的 variant），不再承载 shadcn 默认观感。
- `apps/web/src/index.css` 的 `:root` / `.dark` CSS 变量已改为本文件的颜料盘（HSL 通道三元组，沿用 `hsl(var(--x))` + 透明度修饰符）；Tailwind 沿用，并新增 5 个语义颜料色 + 侧栏 token。
- 字体经 `index.html` 的 Google Fonts `<link>` 加载；`tailwind.config.ts` 配 `font-serif`（Fraunces / 思源宋体）、`font-sans`（Literata / 思源黑体）、`font-mono`（JetBrains Mono），结构边界默认 `border` 宽度提到 2px、`--radius` 归零。
- 组件外观（button / card / table / badge / dialog / select / chart / json-panel / mobile-card）与 layout（侧栏画布边竖栏、列表/详情画框）已按本系统重写；`AuthPage` 等自带 slate/粉彩样式的页面已并入颜料盘。
- **填实色块：** `Badge` 提供 `signal/llm/scheduler/story/cost` 填实语义变体（2px 黑描边、不渐变、不 hover 淡化）；状态 / 类型 / 级别徽章用填实语义色；Auth 额度卡做成填实大色块（按用量绿→黄→玫红）。侧栏选中 = 正黄块，字标用 `--sidebar-brand`。
- **数据依赖：** 「大色块上墙」需要聚合 / 统计数据；`main-agent-context` landing 的统计色块簇需后端补聚合字段才能真实呈现（见 TODOS）。当前前端只在**数据已就绪**处上色块（Auth 额度、各状态徽章、feed 事件/消息标签），不硬编假数。
- 预览样张：`~/.gstack/projects/KisinTheFlame-sparkle/designs/design-system-20260630/preview.html`。

## 决策日志

| 日期       | 决策                               | 理由                                                                                                                                                                                                                                                                                         |
| ---------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-30 | 初版设计系统：晒褪了色的蒙德里安   | `/design-consultation` 产出；三方独立设计意见（主 + Codex + opus 子 Agent）高度收敛后定版；用户审美取向：色彩优先、文艺复兴 / 印象派 / 蒙德里安《Composition with Red, Blue and Yellow》                                                                                                     |
| 2026-06-30 | 弃用 shadcn，保留 Radix 无样式基元 | 用户明确不再用 shadcn；重做的是观感而非交互管线，无需重造可访问性轮子                                                                                                                                                                                                                        |
| 2026-06-30 | 设计系统落地到前端代码             | 颜料盘 / 字体 / 2px 硬线 / 0 圆角 / 侧栏画布边竖栏 / 语义色全部接入 `index.css` + `tailwind.config.ts` + 组件 + layout；AuthPage 等去 slate/粉彩；`pnpm build/typecheck/lint/format` 全绿                                                                                                    |
| 2026-06-30 | 转向「鲜艳大胆 + 二维大色块」      | 用户反馈：晒褪 + 配给把颜色做没了、太灰；要饱和原色 + 大色块上墙 + 二维横竖构图。语义色改蒙德里安原色（红 `#D62818`/蓝 `#143CB0`/黄 `#F7C400`/绿 `#2F8F4E`/玫红 `#B61E3C`），Badge 加填实变体、侧栏正黄选中块、Auth 额度做成大色块。三档 HTML 样例对比后用户选「档 3」。前端先做（不碰后端） |
