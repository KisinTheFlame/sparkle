# 非隐私配置：纳入版本控制，直接改这个文件即可（不再提供 config.yaml.example）。
# 隐私配置（密钥 + PII：apiKey / bot QQ / napcat 群号 / 浏览器凭据）在 gitignored 的
# config.secret.yaml —— 从 config.secret.yaml.example 复制后填写；启动时深合并进本文件。

# 服务拓扑：单机各进程的监听端口与地址的唯一事实来源（见 issue #162）。
# 每个进程从这里读自己的端口和依赖服务的地址；改端口只改这里一处，全网生效。
# host 是「别的服务/网关如何 reach 它」（reachable host），不是绑定地址。绑定地址是各服务
# 代码里的安全决策：卫星进程一律绑 127.0.0.1，只有 gateway（前门）绑 0.0.0.0（issue #274）。
services:
  agent: { host: localhost, port: 20003 } # sparkle-agent
  console: { host: localhost, port: 20006 } # sparkle-console（管理台后端，纯 DB 查询）
  gateway: { host: localhost, port: 20004 } # sparkle-gateway（前门：纯反向代理，/api 分流后端 + 其余转 web；#578）
  web: { host: 127.0.0.1, port: 20016 } # sparkle-web（管理台前端，自持静态托管，仅 localhost：只经 gateway 反代；#578。20015 gba 已占）
  oss: # sparkle-oss（自建对象存储，仅 localhost：agent 经 HttpOssClient 存取，gateway 转只读浏览）
    host: 127.0.0.1
    port: 20005
    databaseUrl: file:./data/oss/oss.db # oss 独占 SQLite 库（blob / object 对象元数据；字节在 data/oss/blobs）
  browser: { host: 127.0.0.1, port: 20007 } # sparkle-browser（仅 localhost：暴露 type secret / eval / screenshot，绝不对外）
  llm: # sparkle-llm（LLM 网关 + OAuth 凭据中心，仅 localhost：agent 经 HttpLlmClient 直连、gateway 转 /auth）
    host: 127.0.0.1
    port: 20009
    databaseUrl: file:./data/llm/llm.db # llm 独占 SQLite 库（llm_chat_call / embedding_cache / claude_file_cache / oauth_*，#539）
  metric: # sparkle-metric（仅 localhost：metric 摄取 + metric-chart 查询；20008 TEI embedding / 20009 sparkle-llm 已占）
    host: 127.0.0.1
    port: 20010
    databaseUrl: file:./data/metric/metric.duckdb # metric 独占 DuckDB 单文件（列式，为 p95/分析聚合而生，#475）；与旧反推路径同址，无需迁移
  spire: { host: 127.0.0.1, port: 20011 } # sparkle-spire（杀塔式卡牌游戏引擎，仅 localhost：agent 经 HttpSpireClient 驱动）
  napcat: # sparkle-napcat（NapCat 接入独立进程，仅 localhost：agent 经 HttpNapcatClient 出站 + SSE 订阅入站；#347。20012 留给像素画 #365）
    host: 127.0.0.1
    port: 20013
    databaseUrl: file:./data/napcat/napcat.db # napcat 独占 SQLite 库（napcat_event / napcat_qq_message / outbox / image_asset，#539）
  pixel: { host: 127.0.0.1, port: 20012 } # sparkle-pixel（像素画服务，仅 localhost：agent 经 HttpPixelClient 驱动）
  gba: # sparkle-gba（GBA 模拟器服务，仅 localhost：agent 经 HttpGbaClient 直连游玩面，gateway 只转 /gba/roms 管理面；#541。20014 scheduler 已占）
    host: 127.0.0.1
    port: 20015
    databaseUrl: file:./data/gba/gba.db # gba 独占 SQLite 库（rom / battery_save / run_state / resume_state；ROM 字节在 OSS）
  scheduler: # sparkle-scheduler（通用定时调度服务，仅 localhost：agent 经 SchedulerClient 注册 + SSE 收 tick；#428。20013 napcat / 20012 pixel 已占）
    host: 127.0.0.1
    port: 20014
    databaseUrl: file:./data/scheduler/scheduler.db # scheduler 独占 SQLite 库（TaskRun 执行历史，#493）
    historyRetentionCount: 200 # 历史 GC：每 (owner, task) 分组保留最近多少条（#493 P2）
    historyRetentionDays: 90 # 历史 GC：删除早于多少天的行（与 count 取交集=更严；running 行永不删）

server:
  databaseUrl: file:./data/agent/agent.db # agent 独占 SQLite 库（#539 拆库），相对仓库根；运行时与 Prisma CLI 解析为绝对路径
  agent:
    contextCompactionTotalTokenThreshold: 600000
    contextCompactionImageCountThreshold: 550 # 上下文图片总数超过该值也触发摘要压缩（图片按张吃上下文，与 token 阈值并列的第二触发条件）
    llmRetryBackoffMs: 30000
    waitToolMaxWaitMs: 600000
    stateSampleIntervalMs: 2000 # 状态心跳采样间隔：每这么久采一次「小镜此刻处于哪个 App / wait / portal」，打 agent.state.sample metric
    notificationLeadingWindowMs: 1000 # 空闲来第一条通知时先开这么长的前沿短窗聚合首批，窗结束才 flush（不再前沿立即发）
    notificationBatchWindowMs: 30000
    messaging:
      aiTone:
        enabled: true # 发言 AI 味门控；置 false 完全退化为原发送行为
        blockThreshold: 0.6 # P(AI) >= 该值则拦下不发，等 confirm_last 二次确认
    asyncTask:
      maxTaskDurationMs: 600000 # 异步工具任务的 manager 级安全超时；超过则以超时结果回流（底层可能仍在跑）
    resource:
      maxBytes: 4194304 # read_resource 入上下文 / send_resource 发图的字节上限（4 MiB）
      fileRoot: ~/sparkle # download_resource/upload_resource/群文件 落盘/读盘沙箱根（默认与 terminal initialCwd 重合）
      fileMaxBytes: 33554432 # 文件桥字节上限（32 MiB，独立于 4 MiB 上下文 cap）
  ithome:
    pollIntervalMs: 300000
    recentArticleLimit: 8
    articleMaxChars: 8000
  napcat:
    reconnectMs: 3000
    requestTimeoutMs: 10000
    startupContextRecentMessageCount: 40
    # blockedGroupIds（屏蔽的 QQ 群号，含专用告警群）是 PII，放 config.secret.yaml；默认参与所有群
  llm:
    timeoutMs: 300000
    authUsageRefreshIntervalMs: 600000
    # 文本向量化：LLM 网关（apps/llm）持有 embedding client，agent 经 HTTP 调用。通用能力配置，与上层能力解耦。
    embedding:
      provider: tei-embedding-gemma
      baseUrl: http://127.0.0.1:20008
      model: google/embeddinggemma-300m
      outputDimensionality: 768
    # 生图：LLM 网关持有 image client，走 openai-codex OAuth（ChatGPT 订阅额度），无需 platform API key。
    # model 是 responses 路由模型（gpt-5.x）；出图后端 gpt-image-2 由 provider 写死。codex 忽略尺寸/质量
    # （固定回 1254×1254），故不在此配 size/quality。
    image:
      provider: openai-codex
      baseUrl: https://chatgpt.com/backend-api/codex/responses
      model: gpt-5.4
    codexAuth:
      enabled: true
      publicBaseUrl: http://localhost:20004
      oauthRedirectPath: /auth/callback
      oauthStateTtlMs: 600000
      refreshLeewayMs: 86400000
      refreshCheckIntervalMs: 300000
      binaryPath: codex
    claudeCodeAuth:
      enabled: true
      publicBaseUrl: http://localhost:20004
      oauthRedirectPath: /callback
      oauthStateTtlMs: 1800000
      refreshLeewayMs: 7200000
      refreshCheckIntervalMs: 300000
    providers:
      deepseek:
        # apiKey 是密钥，放 config.secret.yaml
        baseUrl: https://api.deepseek.com
        models:
          - deepseek-chat
      openai:
        # apiKey 是密钥，放 config.secret.yaml
        baseUrl: https://api.openai.com/v1
        models:
          - gpt-4o-mini
      openaiCodex:
        baseUrl: https://chatgpt.com/backend-api/codex/responses
        models:
          - gpt-5.4
          - gpt-5.4-mini
      claudeCode:
        baseUrl: https://api.anthropic.com
        models:
          - claude-opus-5
          - claude-opus-4-8
          - claude-opus-4-6
          - claude-sonnet-5
          - claude-sonnet-4-6
          - claude-haiku-4-5
        keepAliveReplayIntervalMinutes: 30
        # 图片走 Anthropic Files API（上传拿 file_id，请求体不再随 base64 膨胀撞 ~32MB 上限）。
        # 依赖 OAuth scope 含 user:file_upload——加此 scope 后须重新登录 claude-code。
        # 关掉即回退全 base64。
        useFileApi: true
        # File API 缓存按最近使用时间 GC（#433）。sparkle-llm 每日 04:00 删连续 N 天未使用的图（远端 + 本地）。
        fileCacheGcEnabled: true # kill-switch；false 则不注册 GC task（急停用）
        fileCacheGcMaxIdleDays: 3 # 连续多少天未被使用即回收
        fileCacheGcMaxDeletionsPerRun: 2000 # 单轮删除上限，超出的次日续删
    # 只配 agent / vision 两个 KV 缓存身份。fork 型 task agent（contextSummarizer /
    # todoSuggestionAgent / innerVoice）在调用点直接用 usage=agent，复用主 Agent 前缀命中
    # prompt cache，故不在此单独配置；调用归因走 scene 字段（见 issue #555）。
    usages:
      agent:
        attempts:
          - provider: claude-code
            model: claude-opus-4-6
            times: 3
        # adaptive thinking effort（#573）。usage=agent 覆盖主 Agent 与镜像 fork task agent
        # （它们字节级复用主 Agent 前缀，必须同 lineage）。删掉此行即整体关回 disabled。
        thinking: low
      vision:
        attempts:
          - provider: claude-code
            model: claude-sonnet-5
            times: 2
  # bot.qq / bot.creator 是密钥 / PII，放 config.secret.yaml
  # 自建对象存储（@sparkle/oss）启用开关。地址不在这里——统一来自顶层 services.oss。
  # 整段可省略 → 关闭图片存档（resid 恒为 null，只走 vision 文字描述，优雅降级）；
  # 写出该块即启用，enabled: false 可显式关闭。
  oss:
    enabled: true
  # 每个 App 的配置切片。key 是 App.id，结构由各 App 自己的 configSchema 校验。
  # 没列出来的 App 走 schema 默认值；schema 不接受的字段会在启动时报错。
  apps:
    calc:
      # 计算结果保留的小数位数（0–20）。省略 / null 表示不做四舍五入。
      precision: 6
    terminal:
      # 终端 App。小镜的"家"默认在 ~/sparkle/（由 os.homedir() 解析）。
      # initialCwd: ~/sparkle         # 可选；不填则使用 os.homedir() + "/sparkle"
      commandTimeoutMs: 60000 # 单条命令超时，超时后进程组被强杀，partial 输出仍保留
      previewBytes: 2048 # 工具返回的 stdout/stderr preview 上限，每条独立
      maxOutputBytes: 1048576 # 单条命令完整捕获上限（1 MB），超过后停止接收
      maxCommandLength: 4096 # command 参数字符长度上限
      readOutputMaxSize: 4096 # read_bash_output 单次返回字节上限
      shell: /bin/sh # 执行 bash command 时使用的 shell
    hn:
      # Hacker News App（只读：逛 / 读讨论 / 搜 / 看用户）。两个 API 都零鉴权公开。
      # 整段可省略，全部字段都有默认值。
      userAgent: "Mozilla/5.0 ... Sparkle/1.0" # 请求 HN 用的 User-Agent
      fetchTimeoutMs: 10000 # 单次 HTTP 超时
      fetchMaxAttempts: 3 # 含首次的最大尝试次数（只对 429/408/5xx/网络错误重试）
      fetchBackoffBaseMs: 500 # 指数退避基数
      fetchBackoffMaxMs: 8000 # 退避上限
      glanceDefaultLimit: 15 # glance_hn 默认看几条
      glanceLimitCap: 30 # glance_hn 上限（超过被截断）
      glanceConcurrency: 6 # glance_hn 拉 item 的并发上限
      commentTopLimit: 20 # open_hn_thread 展开的主楼评论数
      commentReplyLimit: 3 # 每条主楼展开的第一层回复数
      perCommentMaxChars: 600 # 单条评论 / 片段最大字符
      responseMaxChars: 8000 # open_hn_thread 评论总字符预算，超出截断
      searchHitsLimit: 15 # search_hn 返回命中数
      userActivityLimit: 10 # open_hn_user 近期发言条数
    amap:
      # 高德地图 App（接入高德 Web 服务 API：geocode / POI / 路径 / 天气 / 静态地图）。
      # apiKey 是凭据，不放这里——填到 config.secret.yaml 的 server.apps.amap.apiKey；
      # 留空（不配 secret）= 优雅降级：App 仍可 enter，但所有工具不可调（canInvoke=false）。
      # 以下都是非隐私调参，整段可省略、全部有默认值。
      fetchTimeoutMs: 10000 # 单次 HTTP 超时
      fetchMaxAttempts: 3 # 含首次的最大尝试次数（只对 429/5xx/网络错误 + 限频/QPS/服务忙 infocode 重试）
      fetchBackoffBaseMs: 500 # 指数退避基数
      fetchBackoffMaxMs: 8000 # 退避上限
      poiPageSize: 10 # POI 每页条数（映射高德 page_size）
      poiPageSizeCap: 25 # page_size 上限（高德硬上限 25）
      aroundDefaultRadiusMeters: 1000 # 周边搜索默认半径(米)，App 默认（≠ 高德 API 默认 5000）
      aroundRadiusCapMeters: 50000 # 周边搜索半径上限(米)
      routeMaxSteps: 30 # 路线分步最多渲染几步，超出截断
      transitMaxPlans: 3 # 公交换乘最多返回几个方案
      responseMaxChars: 8000 # 单次结果进上下文的字符预算，超出截断
      staticMapDefaultSize: "600*400" # 静态地图默认尺寸 '宽*高'，单边 ≤1024
      staticMapScale: 2 # 静态地图清晰度倍数（1 普通 / 2 高清）
    browser:
      # 浏览器行为配置。拆进程后（issue #173）由独立的 sparkle-browser 进程消费；整段可省略，全部字段都有默认值。
      # 只有这 4 个字段是环境相关的；humanize / viewport / 超时 / 截图尺寸等是代码常量，不在此暴露。
      headless: false # 有头/无头。本机带显示用 false 肉眼盯；无显示服务器需 true + Xvfb
      userDataDir: data/browser/default # 持久 profile 目录（登录态跨重启）。相对进程 cwd；sparkle-browser 的 PM2 cwd 为仓库根 → 落仓库根 data/browser/
      # proxy / licenseKey 是凭据，放 config.secret.yaml（server.apps.browser.proxy / licenseKey）
      # 网站账号密码不在配置里：存 SQLite 的 browser_credential 表（Sparkle 运行期可自管），
      # type(secret_handle) 时按 handle 取、明文只在 fill 层停留、永不进上下文。
